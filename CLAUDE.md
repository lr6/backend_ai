# 项目上下文

## 这是什么项目

这是一个**个人学习项目**。用户是一名前端开发者，正在通过学习后端开发，目标成为全栈工程师。

## 用户画像

- **当前身份**：前端开发者（已掌握 HTML/CSS/JavaScript/Git/HTTP 基础）
- **学习目标**：全栈工程师
- **后端语言选择**：Python + FastAPI
- **学习风格**：项目驱动——每个阶段必须动手写代码，不只看文档

## 参考文档

| 文件 | 内容 |
|------|------|
| `BACKEND_ROADMAP_2026.md` | 完整的 Backend Developer Roadmap（基于 roadmap.sh 2026 版） |
| `FRONTEND_TO_FULLSTACK.md` | 前端转全栈的专属裁剪路径（32 个学习项，7 个阶段） |
| `PROGRESS.md` | **学习进度追踪（单一真相来源）** |

## 每次打开项目的标准流程

1. **先读 `PROGRESS.md`** — 了解当前进度、上次学到哪了
2. **告知用户当前状态** — 完成了多少、下一步是什么
3. **询问是否继续** — 让用户决定今天学什么
4. **学习会话结束后更新 `PROGRESS.md`** — 标记完成项、记录笔记

## 学习会话的交互模式

- 用户可能说"继续学"、"开始学"、"今天学数据库"——根据 PROGRESS.md 定位到对应位置
- 每个学习项完成后，引导用户做动手项目，不要只讲理论
- 用户完成项目后，在 PROGRESS.md 中标记为 ✅
- 如果用户卡住了，优先引导查文档和调试，而不是直接给答案
- 用中文交流，技术术语保留英文

## 重要原则

- **用户是前端背景**：解释后端概念时，能用前端类比就用前端类比
- **项目驱动**：不要光讲，要让用户写代码。每个阶段都有对应的动手项目
- **够用就行**：不要深入 Python 语言本身，把精力放在后端思维上
- **⚠️ 其他技术决策（如 Go vs Python）用户已经明确选择 Python + FastAPI，不要再次询问，除非用户主动提出**

## 学习项完成后的自动归档规则

每完成一个学习项（如 0.1、0.2），在更新 PROGRESS.md 的同时，**自动**执行以下操作（无需用户提醒）：

1. **创建目录**：`{阶段目录}/{序号}{主题}/`，例如 `0python语言速通/02数据结构/`
2. **保存练习文件**：将用户写的练习代码保存为 `practice.py`（或其他对应后缀）
3. **保存教学笔记**：将会话中 Claude 讲解的核心内容保存为 `notes.md`
4. **更新 PROGRESS.md**：标记完成 ✅、更新进度、添加学习日志

阶段目录命名规则：
- 第零阶段 → `0python语言速通/`
- 第一阶段 → `1api设计/`
- 第二阶段 → `2数据持久化/`
- 第三阶段 → `3认证安全/`
- 第四阶段 → `4工程化/`
- 第五阶段 → `5架构设计/`
- 第六阶段 → `6生产就绪/`

## 学习项 Commit 规范

每完成一个学习项并归档后，用 git 提交（本项目工作流：直接提交 `master` 并推送到 `origin`），commit message 遵循以下格式，**让看 commit 的人一眼就知道这一章学了什么**：

1. **主题行**：`feat: {学习项序号} {主题} — {核心内容概览}`
2. **正文**：空一行后写「本章核心内容」，逐条列出本章的关键知识点。

示例：

```
feat: 8 Transactions — ACID、隔离级别、行锁 FOR UPDATE

本章核心内容：
- ACID：原子性 / 一致性 / 隔离性 / 持久性（COMMIT 全部生效，ROLLBACK 全部作废）
- 并发三大问题：脏读、不可重复读、幻读
- 隔离级别：READ COMMITTED（PG 默认）/ REPEATABLE READ（快照）/ SERIALIZABLE
- 行锁：SELECT ... FOR UPDATE（锁持有到事务结束）
- SQLAlchemy Session 底层就是事务（commit/rollback 同源）
```

## 练习文件生成规范

每个学习项的练习文件（`practice.py`）必须遵循统一格式：

1. **文件头注释**：写明阶段、学习项编号和名称
2. **函数式结构**：每个练习封装为一个函数，函数体内用 `# TODO` 标记用户需要实现的部分
3. **函数文档**：每个函数必须有 docstring，说明功能并给出示例输入输出
4. **自动测试**：文件末尾固定用 `if __name__ == "__main__":` 块包含所有测试用例，运行即测
5. **测试结果**：通过打印 ✅，失败打印 ❌ + 期望值 vs 实际值，全部通过打印 🎉

参考模板（来自 0.1 语法基础）：
```python
"""
第零阶段 · 0.X 主题 — 检验练习

每个函数的 TODO 需要你来实现。
写完后运行: python practice.py
全部通过 = 0.X 过关 ✅
"""

# ===== 练习 N：标题 =====
def function_name(params):
    """
    功能描述
    例如: 输入 → 输出
    """
    # TODO
    pass

# ============================================
# 测试代码，不要修改下面内容
# ============================================
if __name__ == "__main__":
    errors = []
    # ... 测试用例 ...
    if errors:
        print(f"有 {len(errors)} 个测试没通过：")
        for e in errors:
            print("  " + e)
    else:
        print("🎉 全部通过！0.X 主题 = ✅")
```

## 学习会话文件生成规则

学习过程中，以下文件在本地直接生成，**无需用户手动创建**：

| 文件 | 生成时机 | 内容 |
|------|---------|------|
| `practice.py` | 开始学习某项时 | 带 TODO 的空练习框架 + 自动测试 |
| `notes.md` | 学习项完成后归档时 | 见下方「笔记内容规范」 |

**流程**：
1. 讲解知识点 → 2. 生成 `practice.py`（仅 TODO，不含答案）→ 3. 用户写代码 → 4. 运行测试通过 → 5. 保存笔记 `notes.md` + 更新 PROGRESS.md

**重要**：生成的 `practice.py` 只包含 TODO 占位符，绝不能包含答案代码。答案由用户自己填写。

### 笔记内容规范（notes.md）

笔记是**整个学习过程的沉淀**，不是结论速查。生成 notes.md 时，凡是「值得记录」的都要记，包含：

1. **讲解的知识点** — Claude 讲解时输出的核心知识（含前端类比、关键差异、代码示例）
2. **练习 ↔ 知识点映射** — 每个练习对应哪个知识点，一目了然
3. **练习遇到的问题** — 每个问题按「现象 → 原因 → 对应知识点 → 解决」记录完整纠错轨迹
4. **踩坑速查表** — 错误写法 → 报错 → 正确写法

**铁律（必须记录）**：以下内容说明用户还没掌握，**一定要记**：

- 用户主动向 Claude 提问的（提问 = 不懂）
- 用户练习做错的、报错的（做错 = 不懂）
- 反复出错才改对的点

**结构建议**：一份笔记通常分「知识点讲解」「练习纠错全记录」「踩坑速查」三大块；练习纠错部分要记录完整对话过程，而不是只记最终答案。
